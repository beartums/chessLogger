var toasterApp = angular.module('toasterApp',function(['toasterService']) {
	};
});

toasterApp.service('toasterService',function() {
  var toaster = {};
  return toaster;
});

toasterApp.directive('toasterDirective', function() {
  
});